# ChessQuant Cleanup Kit

This kit removes temporary patch folders, clears Python cache artifacts, moves migration notes into `docs/`, and writes orientation docs so both repositories are newcomer-friendly.

Contents:

- `scripts/cleanup_main_repo.ps1`
- `scripts/cleanup_research_sandbox.ps1`
- `docs/APPLY_COMMANDS.md`

Apply the scripts, then commit the cleaned repositories directly.
